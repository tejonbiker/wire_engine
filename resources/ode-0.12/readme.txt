This resource is created to use Open Dynamics Engine, this proyect is aimed to be used in 
Raspberry Pi (Desktop compatibility is also a concern, but not priority),
to avoid recompiling ODE in other system (it's slow) ODE is included in
binary mode only for Rasberry Pi Debian. 


